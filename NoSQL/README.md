# NoSQL (MongoDB)

Holberton School — MongoDB shell exercises.

## Requirements

- MongoDB server running locally (default `127.0.0.1:27017`).
- Legacy shell: `mongo` (as used in project checks).

## Run a script with the shell

From this directory:

    cat 0-list_databases | mongo

Replace `0-list_databases` with the task file name.
